The trunk isn't really used anymore. I created the branch "new-neo-api" when 
updating to the latest neo-1.0-b6-SNAPSHOT and started to use dependency injection
of EmbeddedNeo instance, instead of using NodeManager.

Someday the "new-neo-api" branch will be merged into trunk.
